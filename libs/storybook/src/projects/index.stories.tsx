import { Meta } from '@storybook/react';
import React from 'react';

const meta: Meta = {
  component: () => <></>,
  args: {},
};
export const S1 = { name: 'prometheus-lab-cncf-project-for-adding-music-and-announcements-to-home' };

export default meta;